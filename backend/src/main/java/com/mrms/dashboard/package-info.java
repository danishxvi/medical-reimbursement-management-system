/**
 * Dashboard module: composes read only figures from the other modules into
 * one payload per role. It owns no data.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Dashboards")
package com.mrms.dashboard;
