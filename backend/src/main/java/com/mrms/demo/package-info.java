/**
 * Demo data for local development only (profile "dev"). Never active in
 * production: the bean is profile guarded and also requires
 * mrms.demo.seed=true.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Demo data (dev only)")
package com.mrms.demo;
